import type { Database } from './storageService'; import type { SavedInsight } from '../types/Insight'; import { nowIso } from '../utils/dates'; import { id } from '../utils/text';
export const saveInsight = (db: Database, insight: Partial<SavedInsight>) => ({ ...db, insights: [{ id:id('ins'), title: insight.title || 'AI snapshot', insight_type: insight.insight_type || 'ask_ai', summary: insight.summary || '', question: insight.question, answer: insight.answer, context_type: insight.context_type, related_call_id: insight.related_call_id, related_issue_id: insight.related_issue_id, raw_json: insight.raw_json, created_at: nowIso() }, ...db.insights] });